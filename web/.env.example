# Base origin of the real backend API. Leave EMPTY to use the built-in Next.js
# mock API routes under /api/*. Set to your Python service origin to go live,
# e.g. NEXT_PUBLIC_API_BASE_URL=https://api.informationhealth.app
NEXT_PUBLIC_API_BASE_URL=
