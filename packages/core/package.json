{
  "name": "@ih/core",
  "version": "0.0.0",
  "private": true,
  "description": "Hidden View's shared client core \u2014 platform-independent product logic for web, Android and iOS.",
  "type": "module",
  "exports": {
    "./*": "./*.ts",
    "./i18n/messages/*.json": "./i18n/messages/*.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "test": "node --test \"**/*.test.ts\""
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22"
  },
  "dependencies": {
    "axios": "^1.7.7"
  }
}
